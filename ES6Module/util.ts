
export function add(a, b) {
    return a + b;
}


export function subtract(a, b) {
    return a - b;
}


//Default can be used to send when we have to semd only one function so on import we dont need { }

export default function multiply(a, b) {
    return a * b;
}